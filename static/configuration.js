const configuration = {
    server: "http://marketing-api.outclass.co.id",
    user: {},
    isLogin: false,
    resetOnStart: false,    //localstorage akan terhapus saat di refresh
    development: true,
    // akan menampilkan setiap api yang dipanggil
    // default selalu mengirimkan ID bukan IDEncrypt
    // setiap api yang dikirim default GodMode=true
}

export default configuration;
